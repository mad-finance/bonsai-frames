// TODO
export const getRemainingBal = async (): Promise<BigInt> => {
  return BigInt(0)
}

// TODO
export const getSize = async (): Promise<BigInt> => {
  return BigInt(0)
}

// TODO
export const getUserAllowance = async (): Promise<BigInt> => {
  return BigInt(0)
}

// TODO
export const getUserHand = async (): Promise<any[]> => {
  return []
}
